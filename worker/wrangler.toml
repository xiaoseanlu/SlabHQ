name = "slabhq-subscribe"
main = "subscribe-worker.js"
compatibility_date = "2024-01-01"

# Set these as secrets via: npx wrangler secret put GITHUB_TOKEN
# and: npx wrangler secret put RESEND_API_KEY

[vars]
GITHUB_REPO = "xiaoseanlu/SlabHQ"
FROM_EMAIL = "SlabHQ Alerts <onboarding@resend.dev>"
